# Antibiogram transcripts

Hand-typed copy of parked PDFs. **Not an AT-12 event.**

- Scope: Temple printed sheets for 2017, 2024, and 2025 only.
- 2017 is printed as Temple University Hospital, **not** Main Campus.
- 2024 and 2025 are printed as Main Campus.
- `IR`, `ID`, `INS`, `-`, and `*` are flags, not percents.
- 2017 printed `0` for many pairs later marked `IR`. Those cells stay `0`.
- Nitrofurantoin is urinary isolates only, as on the sheets.
- Rebuild with `uv run python scripts/build_tuh_main_transcript.py`.

Do not copy this table into `data/derived/event_*/` until an operator names a restriction date and writes a lock file first.
